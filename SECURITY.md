# 安全与数据

- 本版不监听网络端口，无使用遥测，不自动发送学生材料。`--real`会向部署者配置的模型服务发送上下文；运行真实模型前明确数据处理范围。
- 数据库、备份、`.env`与日志禁止提交公开仓库；.gitignore不是权限防护。服务器管理员应设置目录权限、磁盘保护、备份保管和操作访问控制。
- 一个实例是一份独立数据，未提供实例内部多用户权限。不要把CLI包装为无鉴权HTTP接口。不要复制作者或其他学校真实数据做演示。
- API密钥只在私有环境中配置。错误报告不要附完整环境变量、模型响应或真实学生资料。公开Issue使用虚构数据；仓库建立后由维护者配置GitHub私密漏洞报告渠道。
- AI提示词不能作为安全边界。高风险情况先按学校实际危机与保护流程联系真人支持；软件不替代现场责任。
